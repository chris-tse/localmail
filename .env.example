# Google OAuth (optional — only needed when testing real OAuth or release builds)
# Public Localmail-owned desktop OAuth client ID. Gmail uses PKCE; no client secret is used.
GOOGLE_CLIENT_ID=

# Encryption key (optional — derived from machine if not set)
ENCRYPTION_KEY=
